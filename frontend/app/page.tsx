export default function Home() {
  return (
    <main style={{ padding: 32 }}>
      <h1>Onboarding / Offboarding POC</h1>
      <p>
        Starter scaffold. Build out the 7 MVP screens here: Login, Executive
        Dashboard, Employee Directory + Profile, Onboarding Tracker,
        Offboarding Tracker, and Reports Dashboard.
      </p>
      <p>Backend API base URL comes from NEXT_PUBLIC_API_URL (defaults to http://localhost:8000).</p>
    </main>
  );
}
